/**
 * @neuro-endo/ui — Shared UI component library
 *
 * SCOPE NOTE:
 *   React (web) and React Native (mobile) use fundamentally different rendering primitives
 *   (div/span vs View/Text). This package is intended for:
 *     - Logic-bearing components that do not render native primitives directly
 *     - Design tokens (colours, spacing, typography scale)
 *     - Shared hooks (no DOM/RN APIs)
 *
 *   Platform-specific components should live in apps/web or apps/mobile respectively.
 *   Revisit this boundary in Phase 2 after the web MVP is complete.
 *
 * TODO (Phase 2): Add design tokens
 * TODO (Phase 2): Add shared hooks (e.g. useCompatibilityCheck)
 */

// Nothing exported yet — placeholder for Phase 2
export {};
